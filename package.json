{
  "name": "mytab",
  "version": "1.0.0",
  "description": "MyTab — 轻量浏览器新标签页扩展（基于 Litestart，Microsoft 蓝主题，Bing 引擎）",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@eslint/js": "^9.9.1",
    "eslint": "^9.9.1",
    "globals": "^15.9.0",
    "prettier": "^3.3.3"
  }
}
